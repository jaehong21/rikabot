# Rika Web (React + Rsbuild)

React + TanStack Router frontend for Rikabot with static output in `web/dist`.

## Development

```bash
cd web
bun install
bun run dev
```

## Build

```bash
cd web
bun run build
```

Build output is generated in `web/dist` and served by the Rust gateway.
