# -----------------------------------------------------------------------------
# Rikabot config template
# Most settings below show defaults as comments. Uncomment to override.
# Runtime config file resolution:
# 1) RIKA_CONFIG (if set)
# 2) ~/.rika/config.toml (default)
# -----------------------------------------------------------------------------

# host = "127.0.0.1"
# port = 4728
# temperature = 0.1

# workspace_dir = "./workspace" # default: ~/.rika/workspace

# provider is required (no default)
# provider = "openai"
# model = "gpt-5.2"

# [providers.openai]
# base_url = "http://localhost:8317/v1" # default: https://api.openai.com/v1
# env_key = "OPENAI_API_KEY"
# api_key = "xxx"

# [permissions]
# enabled = true

# [providers.openrouter]
# api_key = "YOUR_OPENROUTER_API_KEY"
# env_key = "OPENROUTER_API_KEY"

# [skills]
# enabled = true

# [prompt]
# bootstrap_max_chars = 20000
# bootstrap_total_max_chars = 150000

# [permissions]
# enabled = true

# [permissions.tools]
# allow = [
#   "filesystem_*(*)",
#   "shell(command:cat *)",
#   "shell(command:cd *)",
#   "shell(command:chmod *)",
#   "shell(command:cp *)",
#   "shell(command:curl *)",
#   "shell(command:find *)",
#   "shell(command:ls *)",
#   "shell(command:mkdir *)",
#   "shell(command:rg *)",
#   "mcp_linear_*(*)",
#   "mcp_notion_*(*)"
# ]
# deny = []
# IMPORTANT: when permissions are enabled and allow is empty, policy is default deny.
# allow = [
#   "shell(command:git status *)",
#   "shell(command:npm run *)",
#   "filesystem_read(path:docs/*)"
#   "mcp_linear_*(*)"
# ]
# deny = [
#   "shell(command:git push *)"
# ]

# [mcp]
# enabled = true

# [[mcp.servers]]
# enabled = true
# name = "filesystem"
# transport = "stdio" # "stdio" | "http"
# command = "bunx"
# args = ["-y", "@modelcontextprotocol/server-filesystem", "/tmp"]
# tool_timeout_secs = 180 # max: 600
# init_timeout_secs = 30

# [[mcp.servers]]
# name = "linear"
# transport = "http"
# auth_mode = "oauth" # "headers" | "oauth"
# url = "https://mcp.linear.app/mcp"
#
# For headers mode
# headers = { Authorization = "Bearer ${LINEAR_API_KEY}" }
#
# For oauth mode, client_id is optional when provider supports dynamic client registration.
# oauth_client_id = "your-client-id"
# oauth_client_secret_env = "LINEAR_OAUTH_CLIENT_SECRET"
# oauth_scopes = ["read"]
# oauth_authorization_server = "https://mcp.linear.app"
