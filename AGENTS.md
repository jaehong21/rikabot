# Reference Instruction

- For agent architecture, reference `$HOME/Desktop/open-source/nanobot/`.
- For Rust implementation details, reference `$HOME/Desktop/open-source/zeroclaw/`.
- For most verbose and detailed implementation, reference `$HOME/Desktop/open-source/openclaw/`.

## Testing Requirements

- For all implementation changes, add test code. Do not omit tests.
- Backend changes must include non-trivial test cases that validate realistic logic paths, edge conditions, or failure handling.
- Frontend changes must include tests, and when applicable, add end-to-end(`tests/e2e/`) cases.
